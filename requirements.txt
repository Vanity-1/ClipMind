# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
# 文件上传（FastAPI 的 UploadFile/Form 解析依赖）
python-multipart==0.0.20

# Database
sqlalchemy==2.0.35
aiosqlite==0.20.0

# Async file IO (streaming downloads in async handlers)
aiofiles==24.1.0

# LangChain + vector store
langchain==0.3.7
langchain-community==0.3.7
langchain-openai==0.2.6
langchain-chroma==0.1.4
langchain-huggingface>=0.1.0
langchain-ollama>=0.2.0,<0.3.0
chromadb==0.5.15
# chromadb 运行时核心依赖（显式锁定，避免 CI 构建漂移，PyInstaller 打包需 collect_submodules）
# chroma-hnswlib：HNSW 向量索引后端（C 扩展）
# duckdb：SQL 元数据后端（C 扩展）
# pypika：SQL 构造
# posthog/opentelemetry：chromadb 启动期 import 的遥测模块
# bcrypt：认证（cffi 扩展）
chroma-hnswlib>=0.7.3
duckdb>=0.9.2
pypika>=0.0.1
posthog>=3.0.0
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0
bcrypt>=4.0.0
openai==1.59.7
dashscope==1.20.0

# QR code
qrcode[pil]==7.4.2

# Playwright（抖音扫码登录与同步抓取需要，安装后需执行 `playwright install chromium`）
playwright==1.49.0

# Local ASR (faster-whisper + ctranslate2 C++ backend)
# 必须在 requirements.txt 中显式声明，否则 PyInstaller 打包时不会收集
# faster_whisper / ctranslate2 子模块和动态库，导致安装包内 ASR 功能不可用。
# ctranslate2 是 faster-whisper 的 C++ 推理后端，会作为依赖自动安装。
faster-whisper==1.0.3

# Local Embedding model (bge / m3e via langchain-huggingface)
# 用于模型市场下载的本地向量模型加载，仅在 embedding_provider=local 时使用
sentence-transformers>=3.0.0

# Settings & validation
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Crypto (Cookie / sensitive field encryption at rest)
cryptography>=43.0.0

# Logging
loguru==0.7.2

# Timezone data (PyInstaller frozen 环境下 zoneinfo 需要，缺失会触发 WARNING)
tzdata>=2024.2

# System dependency (required for ASR local fallback)
# ffmpeg (install separately and add to PATH)

# 测试
pytest==8.3.4
pytest-asyncio==0.24.0
